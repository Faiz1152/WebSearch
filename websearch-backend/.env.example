# Example environment variables for local development

# Provide a provider API key if you plan to use a commercial provider
SEARCH_API_KEY=YOUR_API_KEY_HERE

# Optional: restrict which origin the Worker allows (set to your frontend origin)
ALLOWED_ORIGIN=
